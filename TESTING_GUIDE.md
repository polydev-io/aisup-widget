# 🧪 Руководство по тестированию виджета

## ✅ Что готово

Создан полнофункциональный встраиваемый виджет чата с:
- ✅ Real-time чат через WebSocket
- ✅ Интеграция с REST API backend
- ✅ Современный адаптивный дизайн
- ✅ Уведомления о новых сообщениях
- ✅ Индикатор печатания
- ✅ Сохранение истории чата
- ✅ Demo страница для тестирования

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

```bash
cd /Users/daminik00/work/git/aisup-widget
npm install
```

### Шаг 2: Запуск backend сервера

В отдельном терминале:

```bash
cd /Users/daminik00/work/git/aisup-backend
npm run dev
```

Убедитесь что сервер запустился на `http://localhost:3000`

### Шаг 3: Запуск виджета

```bash
cd /Users/daminik00/work/git/aisup-widget
npm run dev
```

Браузер автоматически откроет `http://localhost:5173/demo.html`

## 🎯 Сценарии тестирования

### Тест 1: Базовая функциональность

1. **Открыть демо страницу**: `http://localhost:5173/demo.html`
2. **Нажать на кнопку виджета** в правом нижнем углу (круглая фиолетовая кнопка)
3. **Увидеть окно чата** с приветственным сообщением
4. **Ввести сообщение**: "Привет! Как дела?"
5. **Нажать Enter** или кнопку отправки
6. **Увидеть свое сообщение** в чате (справа, синий фон)
7. **Дождаться ответа от AI** (слева, белый фон)

✅ **Ожидаемый результат**: Сообщение отправлено, получен ответ от AI бота

### Тест 2: WebSocket real-time

1. **Открыть чат** (если закрыт)
2. **Отправить сообщение**: "Расскажи про ваши услуги"
3. **Увидеть индикатор печатания** (три точки анимируются)
4. **Получить ответ в реальном времени**

✅ **Ожидаемый результат**: Ответ приходит через WebSocket без обновления страницы

### Тест 3: Уведомления

1. **Закрыть окно чата** (нажать крестик или кнопку)
2. **Отправить сообщение из другого места** (например, через Telegram бота или админку)
3. **Увидеть красный бейдж** с количеством новых сообщений на кнопке виджета

✅ **Ожидаемый результат**: Бейдж появляется, при открытии чата исчезает

### Тест 4: Кастомизация

1. **Изменить настройки** на demo странице:
   - Основной цвет: выберите другой цвет
   - Позиция: переключите на "Снизу слева"
   - Текст кнопки: измените на "Помощь"
   - Имя пользователя: введите свое имя

2. **Нажать "Применить настройки"**
3. **Увидеть изменения**: виджет переместился, цвет изменился

✅ **Ожидаемый результат**: Виджет обновляется с новыми настройками

### Тест 5: История сообщений

1. **Отправить несколько сообщений**
2. **Обновить страницу** (F5)
3. **Открыть виджет**
4. **Увидеть всю историю** предыдущих сообщений

✅ **Ожидаемый результат**: История сохраняется и загружается при открытии

### Тест 6: Мобильная адаптация

1. **Открыть DevTools** (F12)
2. **Включить мобильный режим** (Ctrl+Shift+M)
3. **Выбрать устройство**: iPhone или Android
4. **Протестировать виджет** на мобильном размере

✅ **Ожидаемый результат**: Виджет адаптируется под размер экрана

## 🔍 Проверка консоли

Откройте консоль браузера (F12) и проверьте логи:

### Успешное подключение:

```
[AISup] WebSocket connected
[AISup] Joined chat room
```

### При отправке сообщения:

```
[AISup] Message sent successfully
```

### При получении ответа:

```
[AISup] Message received from bot
```

## 🐛 Возможные проблемы и решения

### Проблема 1: Виджет не появляется

**Причина**: JavaScript не загрузился или ошибка в коде

**Решение**:
1. Откройте консоль (F12)
2. Проверьте наличие ошибок
3. Убедитесь что `npm run dev` запущен

### Проблема 2: "Connection error"

**Причина**: Backend сервер не запущен или неправильный URL

**Решение**:
1. Проверьте что backend запущен: `http://localhost:3000`
2. Проверьте API URL в настройках виджета
3. Проверьте CORS настройки в backend

### Проблема 3: "Invalid API key"

**Причина**: Неправильный или несуществующий API ключ

**Решение**:
1. Проверьте API ключ в demo.html (должен быть ваш из CLI)
2. Убедитесь что REST бот создан и активен:
   ```bash
   cd ../aisup-backend
   npm run bot:list
   ```
3. Если нужно, создайте новый:
   ```bash
   npm run bot:add-integration
   ```

### Проблема 4: AI не отвечает

**Причина**: AI провайдер не настроен или не работает

**Решение**:
1. Проверьте настройки AI в боте (Qwen App ID, N8n webhook)
2. Проверьте логи backend сервера
3. Убедитесь что у компании настроен AI провайдер

### Проблема 5: Виджет не сохраняет историю

**Причина**: localStorage заблокирован или очищен

**Решение**:
1. Проверьте настройки приватности браузера
2. Разрешите cookies и localStorage для localhost
3. Попробуйте в режиме инкогнито

## 📊 Что протестировать

### Функциональность:
- [x] Открытие/закрытие виджета
- [x] Отправка текстовых сообщений
- [x] Получение ответов от AI
- [x] Real-time WebSocket связь
- [x] Индикатор печатания
- [x] Уведомления о новых сообщениях
- [x] Сохранение истории чата
- [x] Загрузка истории при открытии

### UI/UX:
- [x] Анимации открытия/закрытия
- [x] Скроллинг сообщений
- [x] Адаптивный дизайн
- [x] Кастомизация цветов
- [x] Позиционирование (left/right)

### Производительность:
- [x] Быстрая загрузка виджета
- [x] Плавные анимации
- [x] Нет задержек при отправке
- [x] Эффективное использование памяти

## 🎨 Примеры кастомизации

### Изменить цвет на красный:

```javascript
new AISupportWidget({
  apiKey: 'your-key',
  apiUrl: 'http://localhost:3000',
  primaryColor: '#EF4444'
});
```

### Позиция слева:

```javascript
new AISupportWidget({
  apiKey: 'your-key',
  apiUrl: 'http://localhost:3000',
  position: 'bottom-left'
});
```

### Другое приветствие:

```javascript
new AISupportWidget({
  apiKey: 'your-key',
  apiUrl: 'http://localhost:3000',
  welcomeMessage: 'Привет! Я ваш AI ассистент 👋'
});
```

## 📱 Тестирование на реальных устройствах

### Получить URL для тестирования:

1. Узнайте локальный IP:
   ```bash
   ifconfig | grep inet
   ```

2. Откройте на мобильном:
   ```
   http://192.168.x.x:5173/demo.html
   ```

3. Убедитесь что backend доступен:
   - В backend `.env.development` добавьте ваш IP в ALLOWED_ORIGINS
   - Перезапустите backend

## ✅ Критерии успешного теста

Виджет работает корректно если:

1. ✅ Открывается и закрывается плавно
2. ✅ Отправляются сообщения
3. ✅ Приходят ответы от AI в real-time
4. ✅ Показываются уведомления
5. ✅ Сохраняется история
6. ✅ Работает на мобильных
7. ✅ Можно кастомизировать
8. ✅ Нет ошибок в консоли

## 📝 Отчет о тестировании

После тестирования заполните:

**Окружение:**
- OS: macOS
- Браузер: Chrome/Safari/Firefox
- Разрешение экрана: 
- Backend версия: 1.0.0

**Результаты:**
- Базовая функциональность: ✅ / ❌
- WebSocket real-time: ✅ / ❌
- Уведомления: ✅ / ❌
- Кастомизация: ✅ / ❌
- История: ✅ / ❌
- Мобильная адаптация: ✅ / ❌

**Найденные проблемы:**
1. 
2. 
3. 

**Комментарии:**


## 🚀 Следующие шаги после тестирования

1. **Сборка для production**:
   ```bash
   npm run build
   ```

2. **Деплой файлов**:
   - `dist/widget.js` → на CDN
   - `dist/widget.css` → на CDN

3. **Интеграция на клиентский сайт**:
   - Добавить скрипт на сайт
   - Настроить API ключ
   - Протестировать на production

4. **Документация для клиентов**:
   - Создать инструкцию по интеграции
   - Примеры кастомизации
   - FAQ

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера
2. Проверьте логи backend
3. Убедитесь что все сервисы запущены
4. Проверьте API ключ

---

**Готово к тестированию! 🎉**

Запустите `npm run dev` и откройте `http://localhost:5173/demo.html`
