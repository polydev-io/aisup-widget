# 📱 Поддержка файлов и мобильная версия

## ✅ Что добавлено

### 1. Загрузка файлов 📎

Виджет теперь поддерживает отправку:
- **Изображений** (JPEG, PNG, GIF, WebP)
- **Видео** (MP4, MOV, AVI)
- **Документов** (PDF, DOC, DOCX, TXT)

**Ограничения:**
- Максимальный размер файла: **10 МБ**
- Файлы автоматически загружаются на backend
- Изображения отображаются inline в чате
- Видео можно просматривать прямо в чате
- Документы открываются в новой вкладке

### 2. Мобильная адаптация 📱

- **Полноэкранный режим** на мобильных устройствах (< 480px)
- **Адаптивные размеры** на планшетах (< 768px)
- **Touch-оптимизация** - увеличенные кнопки для сенсорных экранов
- **Предотвращение зума iOS** - font-size 16px для input
- **Responsive изображения** - автоматическая адаптация размера

## 🎯 Как использовать файлы

### Отправка файла пользователем

Просто нажмите на кнопку скрепки 📎 и выберите файл:

1. Откройте чат
2. Нажмите кнопку со скрепкой слева от поля ввода
3. Выберите файл (изображение, видео или документ)
4. Файл автоматически загрузится и отправится

### Отображение файлов

**Изображения:**
- Отображаются inline с возможностью увеличения
- Клик по изображению открывает в новой вкладке
- Поддержка подписей к изображениям

**Видео:**
- Встроенный плеер с controls
- Поддержка всех форматов HTML5 video

**Документы:**
- Иконка файла с названием
- Клик открывает в новой вкладке
- Показывается тип и размер файла

## 📱 Мобильная версия

### Адаптивное поведение

**Десктоп (> 768px):**
- Обычный режим виджета
- Размер окна: 380x600px
- Позиционирование: bottom-right или bottom-left

**Планшет (768px - 480px):**
- Увеличенное окно чата
- Размер: calc(100vw - 40px) x calc(100vh - 120px)
- Позиционирование сохраняется

**Мобильный (< 480px):**
- **Полноэкранный режим** при открытии
- Занимает весь экран (100vw x 100vh)
- Без скруглений и отступов
- Увеличенные кнопки для удобства

**Маленький экран (< 360px):**
- Компактные заголовки
- Оптимизированные отступы
- Меньшая кнопка вызова (52px)

### Touch оптимизация

На сенсорных устройствах:
- Минимальный размер кнопок: **44x44px** (стандарт Apple)
- Увеличенная область нажатия
- Оптимизированные жесты
- Плавная прокрутка

## 🔧 Технические детали

### Backend API

**Endpoint для файлов:**
```
POST /api/integration/send-file
```

**Параметры:**
- `file` (multipart/form-data) - файл
- `chatId` (string) - ID чата
- `caption` (string, optional) - подпись к файлу

**Response:**
```json
{
  "response": "success",
  "message": "Файл успешно загружен и отправлен",
  "data": {
    "url": "http://localhost:3000/uploads/...",
    "type": "photo",
    "originalName": "image.jpg",
    "size": 1024000,
    "mimetype": "image/jpeg",
    "messageId": "..."
  }
}
```

### Типы сообщений

Виджет обрабатывает 4 типа сообщений:
1. `text` - Текстовые сообщения
2. `photo` - Изображения
3. `video` - Видео
4. `file` - Документы и другие файлы

### CSS классы

**Для файлов:**
- `.aisup-file-message` - Контейнер файла
- `.aisup-file-link` - Ссылка на файл
- `.aisup-file-icon` - Иконка файла
- `.aisup-file-info` - Информация о файле
- `.aisup-file-name` - Название файла
- `.aisup-file-size` - Размер файла

**Для изображений:**
- `.aisup-image-message` - Контейнер изображения
- `.aisup-image-caption` - Подпись к изображению

**Для видео:**
- `.aisup-video-message` - Контейнер видео

## 🎨 Кастомизация файлов

### Изменить размер изображений

```css
.aisup-image-message {
  max-width: 320px !important;
}
```

### Изменить стиль файлов

```css
.aisup-file-message {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
  color: white !important;
}
```

### Скрыть подписи

```css
.aisup-image-caption {
  display: none !important;
}
```

## 🐛 Отладка

### Проверка поддержки файлов

Откройте консоль браузера и проверьте логи при загрузке файла:

```javascript
[AISup] File uploaded: {url: "...", type: "photo", ...}
```

### Типичные ошибки

**"Файл слишком большой. Максимум 10 МБ"**
- Решение: Уменьшите размер файла или сжмите изображение

**"Не удалось загрузить файл"**
- Проверьте что backend запущен
- Проверьте права на папку `uploads` в backend
- Проверьте CORS настройки

**Файлы не отображаются**
- Проверьте что `STATIC_URL` правильно настроен в backend
- Убедитесь что файлы доступны по URL

## 📊 Поддерживаемые форматы

### Изображения (type: "photo")
- JPEG/JPG
- PNG
- GIF
- WebP
- SVG
- BMP

### Видео (type: "video")
- MP4
- WebM
- OGG
- MOV (если поддерживается браузером)

### Документы (type: "file")
- PDF
- DOC/DOCX
- TXT
- XLS/XLSX
- ZIP
- И любые другие форматы

## 🚀 Примеры использования

### Программная отправка файла

```javascript
const widget = new AISupportWidget({
  apiKey: 'your-key',
  apiUrl: 'http://localhost:3000'
});

// Получить file объект из input
const fileInput = document.createElement('input');
fileInput.type = 'file';
fileInput.onchange = (e) => {
  const file = e.target.files[0];
  widget.sendFile(file);
};
fileInput.click();
```

### Drag & Drop (будущая функция)

В будущих версиях планируется добавить поддержку drag & drop:

```javascript
// Coming soon
widget.enableDragDrop();
```

## ✅ Checklist для тестирования

### Файлы
- [ ] Загрузка изображения (< 10MB)
- [ ] Загрузка большого изображения (> 10MB) - должна быть ошибка
- [ ] Загрузка PDF документа
- [ ] Загрузка видео
- [ ] Клик по изображению открывает в новой вкладке
- [ ] Видео проигрывается inline
- [ ] История файлов загружается при обновлении

### Мобильная версия
- [ ] Открытие на iPhone (полноэкранный режим)
- [ ] Открытие на Android (полноэкранный режим)
- [ ] Открытие на iPad (адаптивный размер)
- [ ] Все кнопки удобно нажимаются пальцем
- [ ] Нет зума при фокусе на input (iOS)
- [ ] Изображения адаптируются под ширину экрана
- [ ] Плавная прокрутка сообщений

## 🔮 Планы на будущее

- [ ] Drag & Drop для файлов
- [ ] Предпросмотр изображения перед отправкой
- [ ] Прогресс-бар загрузки файлов
- [ ] Поддержка нескольких файлов одновременно
- [ ] Копирование изображений из буфера обмена
- [ ] Галерея для просмотра всех изображений чата

---

**Готово! 🎉**

Виджет теперь полностью поддерживает файлы и оптимизирован для мобильных устройств!
